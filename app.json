{
  "name": "Thunderbit",
  "description": "A web application for digital assets management, with a big emphasis on tagging",
  "keywords": ["digital assets manager", "web application", "Java", "AGPL", "Play! Framework", "cloud", "S3", "multiplatform"],
  "website": "http://thunderbit.github.io/thunderbit",
  "repository": "https://github.com/thunderbit/thunderbit",
  "logo": "http://thunderbit.github.io/thunderbit/images/logo-wb.png"
  "success_url": "/"
  "scripts": {
  },
  "env": {
    "AWS_ACCESS_KEY": {
      "required": true
    },
    "AWS_S3_BUCKET": {
      "required": true
    },
    "AWS_SECRET_KEY": {
      "required": true
    },
    "JAVA_OPTS": {
      "required": true
    },
    "MONGO_URI": {
      "required": true
    },
    "PASSWORD": {
      "required": true
    },
    "SECRET": {
      "required": true
    },
    "USERNAME": {
      "required": true
    }
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/scala"
    }
  ]
}
